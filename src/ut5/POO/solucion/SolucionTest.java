package ut5.POO.solucion;
/*
import org.junit.Test;

import static org.junit.Assert.*;
*/
public class SolucionTest {
/*
    @Test
    public void getPh() {
    }

    @Test
    public void setPh() {
    }

    @Test
    public void clasificar() {
    }

    @Test
    public void toString1() {
    }

    public void testSolucionDouble() {
        double[] datos = {0.1, 1, 1.3, 3, 5.4, 453};
        for (int i = 0; i < datos.length; i++) {
            Solucion s1 = new Solucion(datos[i]);
            Solucion s2 = new Solucion(s1);

            assertEquals(datos[i], s2.getPh(), 0.001);
        }
    }

    @Test
    public void testSetPh() {
        Solucion s = new Solucion();
        s.setPh(1234);
        //Se ha guardado el 1234?
        assertNotEquals(1234, s.getPh(), 0.001);
        //Se ha cambiado el valor?
        assertEquals(0, s.getPh(), 0.001);
        //un valor negtivo
        s.setPh(-0.1);
        //Se ha guardado el 1234?
        assertNotEquals(-0.1, s.getPh(), 0.001);
        //Se ha cambiado el valor?
        assertEquals(0, s.getPh(), 0.001);

    }

    @Test
    public void testClasificar() {
        //AVL
        Solucion s = new Solucion();
        assertEquals("Muy acida", s.clasificar());
        s.setPh(2.99);
        assertEquals("Muy acida", s.clasificar());
        s.setPh(3);
        assertEquals("Acida", s.clasificar());
        s.setPh(3.01);
        assertEquals("Acida", s.clasificar());
        s.setPh(5.99);
        assertEquals("Acida", s.clasificar());
        s.setPh(6);
        assertEquals("Neutra", s.clasificar());
        s.setPh(6.01);
        assertEquals("Neutra", s.clasificar());
        s.setPh(7.99);
        assertEquals("Neutra", s.clasificar());
        s.setPh(8);
        assertEquals("Alcalina", s.clasificar());
        s.setPh(8.01);
        assertEquals("Alcalina", s.clasificar());
        s.setPh(9.99);
        assertEquals("Alcalina", s.clasificar());
        s.setPh(10);
        assertEquals("Muy alcalina", s.clasificar());
        s.setPh(15);
        assertEquals("Muy alcalina", s.clasificar());
    }

 */
}